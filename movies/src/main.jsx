import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
/* 1. createa list of 10 random movies to display on recommended
   2. map over the list displaying the movie components
   3. use bootstrap to style them 
   4. Add a serch bar to look for specific movies*/
